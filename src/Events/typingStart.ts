export default class typingStart {
  private typing: any;
  private client: any;
  constructor(typing: any, client: any) {
    this.typing = typing;
    this.client = client;

    console.log(this.typing);
    
  }
}
